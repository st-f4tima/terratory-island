import base.*;

public class App {
    public static void main(String[] args) throws Exception {
        // not final
        GameManager game = new GameManager(null);
        game.showIntro();
    }
}
